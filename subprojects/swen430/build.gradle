apply plugin: 'application'

dependencies {
    testCompile libraries.junit

    compile fileTree(dir: 'lib/', include: '*.jar')
}

apply from: 'applications.gradle'

mainClassName = 'whilelang.Main'

checkstyle {
    ignoreFailures = true
    showViolations = false
}

